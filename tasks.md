1. Open it in a codespace (in browser, click code, then codespaces, then new codespace on main)
1. In the codespace, show us that you can do the following:
    1. build using cmake
    1. run tests using ctest (from the build directory)
1. Add a github action to do the same
1. Using git, commit and push that action to the repo
1. Verify the action is running
